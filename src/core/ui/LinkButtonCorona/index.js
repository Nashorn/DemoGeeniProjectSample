namespace `core.ui` 
(
    class LinkButtonCorona extends Component {
        static is = "link-button-corona";

        async onConnected() {
            await super.onConnected();
        }
    }
);