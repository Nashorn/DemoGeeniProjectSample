namespace `core.ui` 
(
    class ConnectSocialMedia extends Component {
        static is = "connect-social-media";

        async onConnected() {
            await super.onConnected();
        }
    }
);