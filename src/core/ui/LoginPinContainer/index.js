namespace `core.ui` 
(
    class LoginPinContainer extends Component {
        static is = "login-pin-container";

        async onConnected() {
            await super.onConnected();
        }
    }
);