namespace `core.ui` 
(
    class PaymentPeriodBox extends Component {
        static is = "payment-period-box";

        async onConnected() {
            await super.onConnected();
        }
    }
);