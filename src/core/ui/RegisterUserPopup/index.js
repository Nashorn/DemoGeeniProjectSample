namespace `core.ui` 
(
    class RegisterUserPopup extends Component {
        static is = "register-user-popup";

        async onConnected() {
            await super.onConnected();
        }
    }
);