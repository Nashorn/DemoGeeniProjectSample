namespace `core.ui` 
(
    class TopNavbarTransparentView extends Component {
        static is = "top-navbar-transparent-view";

        async onConnected() {
            await super.onConnected();
        }
    }
);