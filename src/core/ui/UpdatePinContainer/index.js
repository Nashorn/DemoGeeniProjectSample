namespace `core.ui` 
(
    class UpdatePinContainer extends Component {
        static is = "update-pin-container";

        async onConnected() {
            await super.onConnected();
        }
    }
);