namespace `core.ui` 
(
    class MessageBarExtendedChild extends Component {
        static is = "message-bar-extended-child";

        async onConnected() {
            await super.onConnected();
        }
    }
);