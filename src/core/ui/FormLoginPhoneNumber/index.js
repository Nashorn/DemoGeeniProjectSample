namespace `core.ui` 
(
    class FormLoginPhoneNumber extends Component {
        static is = "form-login-phone-number";

        async onConnected() {
            await super.onConnected();
        }
    }
);