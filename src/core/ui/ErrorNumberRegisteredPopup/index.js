namespace `core.ui` 
(
    class ErrorNumberRegisteredPopup extends Component {
        static is = "error-number-registered-popup";

        async onConnected() {
            await super.onConnected();
        }
    }
);