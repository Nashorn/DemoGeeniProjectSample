namespace `core.ui` 
(
    class SelectPaymentPeriod extends Component {
        static is = "select-payment-period";

        async onConnected() {
            await super.onConnected();
        }
    }
);