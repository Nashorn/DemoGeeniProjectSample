namespace `core.ui` 
(
    class MessageBarExtended extends Component {
        static is = "message-bar-extended";

        async onConnected() {
            await super.onConnected();
        }
    }
);