namespace `core.ui` 
(
    class ReconfirmPinForm extends Component {
        static is = "reconfirm-pin-form";

        async onConnected() {
            await super.onConnected();
        }
    }
);